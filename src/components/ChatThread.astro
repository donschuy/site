---
interface Message {
  type: 'sent' | 'received';
  text: string;
  sms: 'sms' | '';
}

interface Props {
  messages: Message[];
}

const { messages } = Astro.props;
---

<div class="chat-container">
  {messages.map((message) => (
    <div class={`imessage-bubble ${message.sms} ${message.type}`}>
      {message.text}
    </div>
  ))}
</div>
